import { animated as a, useSpring, useTransition } from "react-spring";
import dynamic from "next/dynamic";

const TweetTransitions = ({ states }) => {
  return <></>;
};

export default TweetTransitions;
